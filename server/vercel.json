{
  "version": 2,
  "builds": [{
    "src": "src/index.js",
    "use": "@vercel/node",
    "config": { "includeFiles": ["migrations/**"] }
  }],
  "routes": [{ "src": "/(.*)", "dest": "src/index.js" }]
}
